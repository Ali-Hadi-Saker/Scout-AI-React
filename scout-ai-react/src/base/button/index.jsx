import React from "react";
import "./style.css";

const Button = () => {
    return (
    <button
        className={`flex center rounded clickable bold button`}
    >
        Login
    </button>
    );
};

export default Button;
